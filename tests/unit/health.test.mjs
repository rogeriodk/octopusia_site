import test from "node:test";import assert from "node:assert/strict";import {buildHealthPayload} from "../../src/lib/health.js";test("health identifies exact revision",()=>{const p=buildHealthPayload({APP_ENV:"homologacao",GIT_SHA:"abc123"});assert.equal(p.ok,true);assert.equal(p.service,"octopus-ia-site");assert.equal(p.environment,"homologacao");assert.equal(p.commit,"abc123");});
